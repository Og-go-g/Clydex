# Dependencies
node_modules/

# Next.js build
.next/
out/

# Environment variables — PLAINTEXT only. Encrypted (.sops) variants
# are re-allowed below; they are safe to commit.
.env
.env.local
.env.production
.env.development
.env.staging
.env.test
.env.*.local
# Allow the encrypted forms back in.
!*.sops

# Age private keys — local-only, never commit.
*.age-key
keys.txt

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# TypeScript
tsconfig.tsbuildinfo

# Prisma generated client (regenerated on build)
lib/generated/

# Sentry
.sentryclirc

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# Claude project files (session-specific, not code)
.claude/
NEXT_SESSION_PROMPT.md

# Operator-local — never commit
backups/
copy-engine.sh
